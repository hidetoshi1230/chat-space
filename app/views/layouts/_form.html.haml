- if controller.action_name = "new"
  #new_chat_group.new_chat_group
= form_for @group do |f| 
  - if @group.errors.any?
    .chat-group-form__errors
      %h2 1件のエラーが発生しました。
      %ul
        - @group.errors.full_messages.each do |message| 
          %li
            = "グループ名を入力してください。"
  .chat-group-form__field.clearfix
    .chat-group-form__field--left
      = f.label :group_name, "グループ名", class: "chat-group-form__label"
    .chat-group-form__field--right
      = f.text_field :group_name, placeholder:"グループ名を入力してください", class: "chat-group-form__input"
  .chat-group-form__field.clearfix
    .chat-group-form__field--left
      = f.label :name, "チャットメンバーを追加", class: "chat-group-form__label"
    .chat-group-form__field--right
      .chat-group-form__search.clearfix
        %input{ name: "keyword", placeholder: "追加したいユーザー名を入力してください", type: "text", value:"", class: "chat-group-form__input search"}
      .user_search_result


  .chat-group-form__field.clearfix
    .chat-group-form__field--left
      = f.label :user_name, "チャットメンバー", class: "chat-group-form__label"
    .chat-group-form__field--right
      .chat-group-users
  .chat-group-form__field.clearfix
    .chat-group-form__field--left
    .chat-group-form__field--right
      -if controller.action_name = "new"
        = f.submit "登録する", class: "chat-group-form__action-btn"
      - else
        = f.submit "Save", class: "chat-group-form__action-btn"
